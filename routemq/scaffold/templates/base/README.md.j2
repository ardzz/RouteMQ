# {{ project_name }}

RouteMQ starter project generated by `routemq new`.

## Features

Selected features: {{ feature_list }}

## Quick start

```bash
cd {{ project_name }}
{% if package_manager == "uv" -%}
uv sync
uv run routemq run
{% else -%}
pip install -e .
routemq run
{% endif -%}
```

Routes live in `app/routers/`; handlers live in `app/controllers/`.

{% if with_queue -%}
## Queue worker

```bash
routemq queue-work --queue default
```
{% endif -%}
