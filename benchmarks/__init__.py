"""Opt-in stdlib benchmark harnesses for RouteMQ.

Run explicitly with ``uv run python -m benchmarks.<module>``. These modules are
kept outside ``tests/`` so normal unit discovery never executes them.
"""
